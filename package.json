{
  "name": "beetasky",
  "private": true,
  "scripts": {
    "dev": "concurrently -c \"#60a5fa,#a78bfa,#34d399\" \"turbo dev\" \"cd backend && php artisan serve\" \"cd backend && php artisan queue:listen --tries=3\" --names=apps,serve,queue --kill-others",
    "dev:apps": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.1"
}

